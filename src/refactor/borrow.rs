
/// Only takes a file path as the non_local_controller has already setup the
/// final output filepath. 
pub fn borrow(
    file_path: &str,
    calle_fn_name: &str,
    caller_fn_name: &str,
) -> bool {
    todo!()
}