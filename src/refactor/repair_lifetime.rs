use std::time::Instant;
use log::info;

pub fn repair_lifetime(
    file_path:  &str,
    callee_fn_name: &str,
    caller_fn_name: &str,
    backup:         &str,
) -> bool {
    todo!()
}

fn repair_lifetime_rustc(

) -> bool {
    todo!()
}

fn repair_lifetime_cargo(

) -> bool {
    todo!()
}